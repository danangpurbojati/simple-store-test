import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    pageWrapper: {
        marginTop: '24px',
    },
    title: {
        fontWeight: 'bold',
        marginBottom: '16px'
    }
});

export default useStyles;